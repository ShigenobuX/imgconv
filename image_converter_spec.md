# 【開発仕様書】Windows向け 画像フォーマットコンバーター (HEIC対応・右クリックメニュー連動)

## 1. プロジェクト概要
Windows環境で動作する、デスクトップ形式の画像フォーマット変換アプリケーション。
iPhoneの標準フォーマットである **HEIC/HEIF** を含む多様な画像を読み込み、指定フォーマット（JPG, PNG, WEBP等）に一括変換する。
GUI操作に加え、**エクスプローラーの右クリック（コンテキストメニュー）からGUIを開かずに直接変換できる機能**を搭載する。

---

## 2. 技術スタック
- **言語:** Python 3.11+
- **GUIフレームワーク:** `customtkinter` (Windows 11風モダンUI)
- **ドラッグ＆ドロップ機能:** `tkinterdnd2-universal` (または `TkinterDnD2`)
- **画像処理ライブラリ:** `Pillow` (PIL)
- **HEICデコーダー:** `pillow-heif`
- **Windows通知機能:** `win11toast`
- **レジストリ操作 (右クリックメニュー):** Python標準 `winreg` モジュール
- **ビルド・exe化:** `PyInstaller`

---

## 3. 機能要件

### 3.1 ファイル入力
- **対応入力フォーマット:** `HEIC`, `HEIF`, `PNG`, `JPG/JPEG`, `WEBP`, `BMP`, `TIFF`, `GIF`, `ICO`
- **読み込み方法:**
  1. ウィンドウ内エリアへのドラッグ＆ドロップ
  2. 「ファイルを選択」「フォルダを選択」ダイアログからの追加
  3. エクスプローラーの右クリックメニュー（CLI引数経由）

### 3.2 変換設定 (GUI)
- **出力フォーマット:** `JPG`, `PNG`, `WEBP`, `ICO`, `BMP` から選択（ラジオボタンまたはドロップダウン）
- **画質設定 (JPG/WEBP用):** スライダー（1～100% / デフォルト: 85%）
- **Exif（撮影日時・位置情報など）設定:**
  - チェックボックス: 「Exif情報を保持する」（デフォルト: ON）
- **リサイズ設定 (オプション):**
  - アスペクト比維持機能付きの幅・高さ指定（ピクセルまたはパーセント）

### 3.3 保存設定
- **保存先:**
  - オプションA: 「元ファイルと同じフォルダ」（デフォルト）
  - オプションB: 「指定フォルダ」（フォルダ選択ダイアログ）
- **同名ファイルの衝突回避:**
  - 自動連番付与（例: `photo_1.jpg`）

### 3.4 実行とフィードバック (GUI)
- **変換実行ボタン**
- **プログレスバー:** 全体進捗の可視化
- **ステータス表示:** 処理中のファイル名、成功・失敗ログのリアルタイム表示

---

### 3.5 右クリック（コンテキストメニュー）連動機能（CLIモジュール）
- **GUI設定画面への項目追加:**
  - 「右クリックメニューに登録する」/「解除する」の切り替えボタン
  - 実行時は管理者権限（UAC）をチェックし、レジストリへ登録を実施
- **登録されるメニュー:**
  - `画像フォーマット変換` ＞ `JPGに変換 (品質85%)`, `PNGに変換`, `WEBPに変換`
- **CLI引数動作:**
  - 例: `app.exe --convert-to jpg "C:\path\to\image.heic"`
  - 上記引数で起動された場合、GUIを生成せずバックグラウンドで高速変換を実行。
  - 変換完了時、`win11toast` を利用して「〇件の変換が完了しました」とWindowsトースト通知を表示。

---

## 4. UI/UX 設計要件
- **全体デザイン:** Windows 11準拠のダークモード / ライトモード対応
- **レイアウト構成:**
  1. **上部:** ドラッグ＆ドロップエリア（点線枠）およびファイル一覧（リストビュー）
  2. **中部:** 変換形式、画質、Exif、リサイズの各設定パネル
  3. **下部:** 保存先指定、変換開始ボタン、プログレスバー
  4. **ヘッダー/設定:** 右クリックメニュー登録ボタン、テーマ切り替え

---

## 5. 非機能要件・技術的注意事項 (AIコーディング指定)

1. **HEICの初期化:**
   アプリ起動時に必ず以下を実行すること。
   ```python
   import pillow_heif
   pillow_heif.register_heif_opener()
   ```
2. **ノンブロッキング処理 (マルチスレッド):**
   HEICのデコードや大量変換時にGUIが応答なしにならないよう、変換処理は必ず `threading.Thread` を使用してバックグラウンドで実行すること。
3. **レジストリ登録の安全仕様:**
   - 登録先キー: `HKCU\Software\Classes\*\shell`（ユーザー権限で書き込み可能な場所を優先）
   - コマンドパスは `sys.executable`（Pythonスクリプト実行時）または `sys.argv[0]`（PyInstallerビルド時）を判定して正しく設定すること。
4. **エラーハンドリング:**
   - 破損ファイルや未対応フォーマット読み込み時の個別スキップとログ記録。

---

## 6. 実装ステップ計画

AIに実装させる際は、以下のステップ順に指示を出すこと。

- **Step 1:** 環境初期化コードとHEIC動作検証スクリプトの作成
- **Step 2:** CustomTkinterによる基本GUIフレームワークとドラッグ＆ドロップの実装
- **Step 3:** Pillow + pillow-heif を使った画像変換コアロジックの実装
- **Step 4:** バックグラウンド処理（Thread）とプログレスバー・ログの連動
- **Step 5:** CLI引数処理、右クリックメニュー（レジストリ操作）、Windows通知機能の実装
- **Step 6:** PyInstallerを使った `.exe` 化（単一ファイル実行化）設定
